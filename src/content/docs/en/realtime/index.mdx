---
title: Realtime
description: "Add WebSocket live sync with a Redis pub/sub backplane so every board member sees changes instantly."
sidebar:
  order: 0
---

Add WebSocket live sync with a Redis pub/sub backplane so every board member sees changes instantly.

Start with [The protocol →](/taskflow/en/realtime/protocol/), then work through the lessons in this module in order (they're listed in the sidebar).
