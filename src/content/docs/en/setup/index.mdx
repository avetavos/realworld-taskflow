---
title: Setup & Tooling
description: "Set up the monorepo, the Rust (Cargo) workspace, the Astro app, and a first Docker Compose file running Postgres and Redis."
sidebar:
  order: 0
---

Set up the monorepo, the Rust (Cargo) workspace, the Astro app, and a first Docker Compose file running Postgres and Redis.

Start with [Repo layout →](/taskflow/en/setup/repo-layout/), then work through the lessons in this module in order (they're listed in the sidebar).
