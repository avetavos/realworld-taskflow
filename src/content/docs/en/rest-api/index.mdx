---
title: REST API
description: "Build the REST CRUD for boards, columns, cards, and labels, plus the fractional-position card move endpoint."
sidebar:
  order: 0
---

Build the REST CRUD for boards, columns, cards, and labels, plus the fractional-position card move endpoint.

Start with [API design →](/taskflow/en/rest-api/design/), then work through the lessons in this module in order (they're listed in the sidebar).
