---
title: Caching
description: "Add a Redis cache-aside layer over board reads, invalidate it on writes, and rate-limit the auth endpoints."
sidebar:
  order: 0
---

Add a Redis cache-aside layer over board reads, invalidate it on writes, and rate-limit the auth endpoints.

Start with [Caching reads →](/taskflow/en/caching/cache-reads/), then work through the lessons in this module in order (they're listed in the sidebar).
