{
  "name": "realworld-taskflow",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && rm -rf .cf-assets && mkdir -p .cf-assets/taskflow && cp -R dist/. .cf-assets/taskflow/ && npx wrangler deploy",
    "deploy:projects": "./deploy-projects.sh"
  },
  "dependencies": {
    "@astrojs/preact": "^5.1.5",
    "@astrojs/starlight": "^0.40.0",
    "astro": "^6.4.5",
    "preact": "^10.29.2",
    "sharp": "^0.34.5"
  }
}
